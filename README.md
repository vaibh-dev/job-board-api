# job-board-api
A production-style REST API built with Java and Spring Boot that allows employers to post jobs and jobseekers to discover and apply to them — with JWT authentication, role-based access control, and a one-command Docker deployment.
